# Sakamichi Box rMVP 제품 줄기와 가지

기준일: 2026-08-25

## 1. 큰 줄기

Sakamichi Box의 rMVP는 국적이나 사용 언어와 관계없이 사용자가 기억하는 최소 단서 하나—멤버 이름, 성, 이름, 별명, 한자·가나·한글·로마자 표기, 그룹명—로 신뢰 가능한 멤버 페이지를 찾고 공식 링크로 이동하게 하는 다국어 아이돌 디렉터리다.

핵심 흐름은 `검색엔진 또는 사이트 검색 → 정확한 그룹·멤버 식별 → 데이터 범위와 출처 확인 → 공식 채널 이동`이다. 콘텐츠 양이나 광고 노출보다 이름 식별 정확성, 데이터 완성도 공개, 모바일 가독성을 우선한다.

## 2. 사용자와 문제

- 일본어 사용자: 한자 일부, 성 또는 가나만 기억해도 찾을 수 있어야 한다.
- 한국어 사용자: 한글 표기, 초성, 성·이름 일부, 통용 별명으로 찾을 수 있어야 한다.
- 해외 사용자: 로마자 순서, 공백·하이픈 차이, 그룹 영문명으로 찾을 수 있어야 한다.
- 공통 문제: 일부 그룹은 데이터가 부분 수집인데 UI가 전체 명단처럼 보이고, 랭킹은 데이터 연결 없이 완성된 기능처럼 보인다.

## 3. 가지

### A. 다국어 발견과 검색 유입

- 멤버 검색어: 한자, 가나, 한글, 초성, 로마자, 별명, 성/이름 분리형, 공백·하이픈 제거형.
- 그룹 검색어: 일본어·한국어·영문 그룹명과 약칭을 멤버 검색 인덱스에 연결한다.
- 검색엔진: 멤버 페이지의 제목·설명·본문·Person JSON-LD에 동일한 검증된 이름 변형과 그룹명을 노출한다.
- 금지: 검색 유입만 노린 얇은 별칭별 doorway 페이지를 대량 생성하지 않는다.

### B. 데이터 신뢰와 완성도

- 그룹별 멤버·사진·공식 링크·이벤트·디스코그래피 커버리지를 표시한다.
- `complete / partial / collecting`과 마지막 확인일을 사용자에게 공개한다.
- 외부 URL 접근 실패를 인물 오염이나 삭제 근거로 사용하지 않는다.

### C. 핵심 기능 정합성

- 데이터가 전달되지 않는 랭킹 패널은 rMVP에서 제거한다.
- 검증된 다중 플랫폼 스냅숏과 자동 갱신이 생기기 전에는 가짜 탭과 수동 순위를 노출하지 않는다.
- 빈 상태는 `예정 없음 / 수집 미지원 / 수집 중 / 갱신 실패`를 구분한다.

### D. UX와 정보 구조

- 홈은 선택한 계열의 핵심 콘텐츠를 우선하고 반복 레일의 탐색 피로를 낮춘다.
- 검색 결과에 그룹명과 일치 근거를 표시한다.
- 모바일 보조 정보는 12px 이상을 기본으로 하고 가로 레일에 다음 항목 단서를 제공한다.

### E. 시각 디자인

- Panel / Card / Tile 세 계층의 반경·테두리·그림자 토큰을 통일한다.
- 배경은 paper+grain을 기본으로 하고 blob/aqua 효과 강도를 화면 크기와 모션 설정에 맞춰 낮춘다.
- LIVE 포스터가 없거나 TikTok 채널에 개인 사진을 확실히 제공할 수 없을 때는 빈 원·문자만 노출하지 않고, 해당 멤버·이벤트가 속한 그룹의 출처가 명시된 로고를 식별용 대체 이미지로 사용한다.
- 다크 모드는 의미 기반 토큰 정리 이후의 가지로 둔다.

### F. 광고와 안전

- 자동 광고가 내비게이션·검색 결과·카드 클릭 영역을 가리지 않도록 모바일 실측한다.
- CMP와 정책 센터는 관리 화면에서 확인하고 확인 전 완료로 표기하지 않는다.

### G. 비공개 문의와 계정 경계

- 운영자의 개인 이메일 주소를 공개 HTML·정책 문서·안내 문서에 노출하지 않는다.
- 회원 기능이 없는 제품 범위에 맞춰 가입·로그인 없이 문의를 작성하게 한다.
- 문의 등록 시 사용자가 임시 문의 아이디와 비밀번호를 직접 정한다. 이는 회원 계정이 아니며 프로필·세션·가입 절차를 만들지 않는다.
- 일반 사용자는 `임시 아이디 + 비밀번호`가 모두 맞는 자신의 문의들만 조회할 수 있고, 다른 조합의 제목·본문·상태·식별자는 조회할 수 없다.
- 운영자는 Supabase Dashboard의 보호된 관리 환경에서 전체 문의를 읽고 답변한다. 공개 사이트에는 관리자 전체 목록 API나 운영자 비밀번호 입력면을 만들지 않는다.
- 임시 아이디와 문의 비밀번호 원문은 저장하지 않고 PostgreSQL `pgcrypto`의 단방향 해시만 저장한다. `anon`·`authenticated` 공개 역할에는 문의 테이블과 RPC 직접 실행 권한을 주지 않으며, Vercel 서버 API가 server-only secret으로 service-role 전용 등록·조회 RPC를 호출한다.
- 임시 아이디 해시와 문의 내용의 수집·보존·삭제 범위를 개인정보처리방침에 명시한다.

## 4. 이번 rMVP 실행 범위

1. 검색 인덱스에 그룹 다국어명·검색 파생어를 포함한다.
2. 멤버 페이지에 별칭·검색 가능한 이름 변형·출처 확인일을 보이는 정보로 추가한다.
3. 멤버 메타데이터와 JSON-LD의 `alternateName`을 같은 유틸리티로 생성한다.
4. 그룹 페이지에 데이터 커버리지 패널을 추가한다.
5. 연결되지 않은 랭킹 패널을 제거한다.
6. 검색 결과에 그룹명을 표시하고 카드/보조 글자 계층을 정리한다.
7. 카드와 배경의 공통 시각 토큰을 보완한다.

## 5. 이번 범위에서 하지 않는 것

- 근거 없는 별명 생성, 유료 SNS API, 취미·특기 추정, 별칭별 SEO 페이지, 광고 위치 자동 변경, 전체 다크 모드.
- 회원가입·로그인, 공개 문의 목록, 사용자 간 문의 열람, 이메일 주소 공개, 비밀번호 원문 저장, 첨부파일 업로드, 자동 이메일 알림.

## 6. 정합성 후속 실행 — 성능·실패 상태·문의 방어

- 큰 줄기: 다국어 검색과 공식 링크 이동 흐름을 유지하면서 홈 첫 응답을 가볍게 하고, 외부 이미지와 무회원 문의가 실패해도 전체 신뢰를 훼손하지 않게 한다.
- 홈 데이터: 클라이언트 홈 UI에는 생일·채널·그룹 인원 표시에 실제 사용하는 멤버 필드만 전달한다. 검색·상세 페이지의 전체 멤버 데이터는 바꾸지 않는다.
- 미디어: LIVE 포스터 URL이 없을 때뿐 아니라 원격 이미지가 깨질 때도 그룹 로고/브랜드 대체 카드로 전환한다.
- 문의 안전: 임시 아이디 UX는 유지하되 동시 등록 제한을 원자적으로 만들고, 동일 아이디에 행을 무제한 쌓아 조회 bcrypt 비용을 키우지 못하게 제한한다. CAPTCHA 또는 네트워크 단위 방어가 연결되기 전에는 운영 활성화를 완료로 간주하지 않는다.
- 문서 경계: 계정 방식과 자동 문의번호 방식은 폐기 이력으로만 남기고 최종 임시 아이디 방식과 혼동되지 않게 표시한다.
- 바꾸지 않는 범위: 회원가입, 사용자 추적용 영구 계정, 운영자 공개 UI, 검색 결과 축소, 멤버 상세 데이터 삭제는 도입하지 않는다.

## 7. 운영 연결 실행 — 문서 신뢰와 전용 문의 인프라

- 큰 줄기: 공식 정보 탐색은 계속 정적·공개 흐름으로 유지하고, 비공개 문의만 별도 Supabase 프로젝트에 최소 권한으로 연결한다.
- 사용자 의도: 과거 계획·인수인계가 현행 상태로 오인되지 않게 문서 권위를 명확히 하고, 두 번째 Supabase 조직의 전용 프로젝트에서 무회원 문의를 실제 검증한다.
- 문서 가지: `README.md`가 가리키는 현행 문서와 역사 문서를 구분하고, 역사 문서 내부의 실행 지시·수치가 현재 지시처럼 읽히지 않게 표시한다.
- UI 가지: 문의 안내는 세 언어 모두 회원 계정이 아니라 사용자가 정한 임시 아이디와 비밀번호임을 분명히 한다.
- 데이터·명령 가지: `seceond Organization` 안에 `sakamichi-box` 전용 서울 리전 프로젝트를 생성·연결하고 기존 Kurashi 및 `smarttube-box` 프로젝트는 변경하지 않는다.
- 안전 경계: migration·pgTAP·advisor, publishable key의 직접 RPC 거부 검증, server-only API smoke test가 통과하기 전에는 Vercel 환경 변수를 연결하거나 운영 문의를 활성화했다고 표시하지 않는다. DB 비밀번호, secret/service-role key, 개인 문의 원문은 저장소와 인수인계에 기록하지 않는다.
- 네트워크 방어 가지: 브라우저에는 DB RPC 실행 권한을 주지 않는다. 문의 등록·조회는 Vercel 서버 API를 통해서만 수행하고, 해당 경로에 IP 기준 rate limit을 단계적으로 적용한다. 서버 전용 Supabase secret은 Vercel의 server-only 환경 변수에만 저장한다.
- 실패·빈 상태: 프로젝트 생성이나 DB 검증이 실패하면 공개 사이트의 `설정 중` 상태를 유지하며, 생성된 프로젝트를 임의 삭제하거나 다른 프로젝트로 대체하지 않는다.
- 바꾸지 않는 범위: 회원가입·세션·공개 관리자 UI, 다른 제품의 Supabase 프로젝트, 광고 배치, 멤버·이벤트 데이터는 변경하지 않는다.

## 8. 운영 활성화 후 정합성 마감

- 큰 줄기: 문의 기능은 신뢰·운영을 보조하는 가지로 동결하고, 다음 제품 투자는 다국어 검색 정확도와 공식 링크·데이터 최신성으로 되돌린다.
- 문서 가지: 진입점과 역사 인수인계에 표시된 빌드·데이터 수치를 검증된 현재 값과 맞추고, 역사 문서는 현재 실행 계약으로 읽히지 않게 유지한다.
- UI 가지: 활성화된 문의 등록·조회 탭을 키보드와 보조 기술로 구분할 수 있게 탭 상태와 패널 관계를 명시한다.
- 검증 가지: 외부 계정·요금·운영 설정을 바꾸지 않고 로컬 타입 검사, 데이터·검색 검증, 프로덕션 빌드와 브라우저 회귀 검증을 수행한다.
- 바꾸지 않는 범위: Supabase/Vercel 설정, Firewall 규칙, 운영 데이터, 광고 설정, 문의 스키마와 공개 API 계약은 변경하지 않는다.

## 9. 무료 운영 유지와 데이터·시각 신뢰 자동화

- 비용 경계: Vercel Hobby 요금제를 유지하며 유료 Firewall·SNS API·도메인 기능을 전제로 설계하지 않는다.
- 저장 경계: 새 서버 저장이 꼭 필요할 때만 두 번째 조직의 기존 `sakamichi-box` Supabase 프로젝트를 사용한다. 이번 실행에서는 새 테이블·프로젝트·운영 데이터를 만들지 않는다.
- 링크 신뢰: 링크 점검의 기본 동작은 보고서 생성만 수행한다. 타임아웃·DNS·봇 차단 같은 네트워크 실패는 `dead`가 아니라 `unverified`로 분류하고, 명시적 쓰기 옵션 없이 `members.json`을 변경하지 않는다.
- 데이터 가지: 그룹별 멤버·사진·공식 링크·확인일·이벤트·디스코그래피 커버리지를 로컬 보고서로 재현 가능하게 측정한다.
- UI 가지: 사용자 스크린샷에서 보인 그룹 카드의 비정상 축소·겹침과 LIVE fallback 로고의 낮은 식별성을 우선 회귀 항목으로 둔다. 카드 전체 폭·높이·텍스트 흐름과 로고 가용 면적을 실제 뷰포트에서 검증한다.
- UI·광고 가지: 모바일 핵심 내비게이션·검색·카드·문의 흐름을 무료 로컬 브라우저 검증으로 확인한다. 동적 AdSense 실광고가 제공되지 않는 환경의 한계는 통과로 가장하지 않는다.
- 운영 가지: 기존 GitHub 자동 갱신, Vercel 배포, Supabase advisor는 읽기 전용으로 상태를 확인하며 workflow 실행·배포·스키마 변경은 별도 승인 없이는 수행하지 않는다.
- 다음 투자: 무료 운영 안정성과 데이터 신뢰를 먼저 유지하고 discography UI·통계·다크 모드는 후속 제품 확장으로 남긴다.

## 10. 문서 권위·보안 계약 정합화

- 사용자 의도: 다음 작업자가 역사 문서의 과거 실행 지시나 수치를 현행 계약으로 오인하지 않고, 문의 보안 경계와 현재 검증 기준을 한 번에 식별하게 한다.
- 큰 줄기: 공식 정보 탐색과 무료 운영 경계는 유지하며, 문서만 현행 `README.md`의 권위 지도와 운영 완료 상태에 맞춘다.
- 보안 가지: 공개 역할에는 문의 테이블과 RPC 직접 실행 권한을 주지 않고, 브라우저는 Vercel 서버 API만 호출하며 서버가 server-only secret으로 service-role 전용 RPC를 실행한다.
- 문서 가지: 폐기 문서의 탈출 링크를 현행 제품 기획·작업지시·운영 인수인계로 통일하고, 역사 문서의 과거 수치는 `당시 기준`으로 표시한다.
- 실패·회귀 경계: 과거 기록 자체를 삭제하거나 코드·데이터·Supabase·Vercel·광고·배포 상태를 변경하지 않는다. 현행 수치와 당시 수치를 혼합하지 않는다.
- 완료 조건: 공개 RPC 허용 문구가 남지 않고, 현재 1,538경로와 당시 1,536페이지가 명확히 구분되며, 폐기 문서가 현행 3문서를 정확히 안내한다.
- 인수인계 가지: `CODEX_HANDOVER.md`의 현재 상태에 정합화 완료 사실과 문서 권위 순서를 기록해, 다음 작업자가 역사 문서보다 현행 제품 기획·작업지시·운영 상태를 먼저 읽게 한다.

## 11. 전체 정합성 복구와 배포 빌드 정상화

- 사용자 의도: 현재 기능을 제품 큰 줄기와 다시 맞추고, 자동 수집 데이터 때문에 검증과 Vercel 빌드가 중단되지 않게 한다.
- 데이터 가지: 이벤트 공식 URL은 HTTPS 계약을 지키며, 수집기가 HTTP 링크를 그대로 저장하지 않도록 생성 지점에서 정규화하거나 안전하게 제외한다. 현재 데이터 수치는 검증 결과를 기준으로 문서와 동기화한다.
- 검색 가지: 로마자 공백·하이픈 차이를 같은 검색어로 취급하고, 검색 결과에는 그룹명뿐 아니라 사용자 질의가 어느 이름·별칭·그룹명과 일치했는지 설명 가능한 근거를 제공한다.
- 신뢰 UI 가지: 그룹의 `complete / partial / collecting`은 단순 링크 존재 여부가 아니라 명시적 수집 범위와 렌더 가능한 공식 링크를 기준으로 보수적으로 표시한다.
- 배포 가지: 로컬 검증을 먼저 통과시킨 뒤 마지막 단계에서 Vercel의 실제 실패 로그를 확인하고, 저장소 안에서 재현 가능한 원인만 수정한다. 환경 변수·Firewall·Supabase 운영 데이터는 원인으로 확인되지 않는 한 변경하지 않는다.
- 바꾸지 않는 범위: 검증되지 않은 인물·별칭·URL 생성, 유료 기능 도입, 기존 사용자 변경 되돌리기, 운영 데이터 임의 삭제, 제품 범위 확장은 하지 않는다.

## 12. 전체 멤버 공식 프로필 재감사와 갱신 자동화

- 사용자 의도: 공식 사이트에 생일·사진·Instagram 등 정보가 존재하는데 로컬 데이터에 빠진 사례를 454명 전체에서 다시 찾아 보강하고, 이후 자동 갱신에서도 같은 누락이 반복되지 않게 한다.
- 출처 우선순위: 그룹 공식 프로필·공식 소속사·본인 또는 그룹이 연결한 공식 SNS를 1순위로 사용한다. 공식 사이트 접근이 막힌 경우에만 Wikimedia/Wikipedia를 발견 보조 및 교차 확인에 사용하며 검색 결과만으로 인물·계정을 확정하지 않는다.
- 데이터 묶음: 이름·그룹·활동 상태·생일·사진·공식 프로필·Instagram/X/YouTube/TikTok·출처 URL·확인일을 한 인물 단위로 대조한다.
- 안전 경계: 동일 인물임이 확인되지 않은 사진과 SNS는 추가하지 않는다. 네트워크 실패·403·봇 차단은 삭제나 `dead` 판정 근거로 사용하지 않으며 기존 검증 데이터를 자동으로 비우지 않는다.
- 자동화 가지: 그룹별 수집기는 발견과 검증을 분리하고, 변경 전후 감사 보고서와 데이터 검증을 통과한 경우에만 결과를 저장한다. 바탕화면 업데이트는 저장소의 단일 배치 파일을 원본으로 삼고 동일한 검사 순서와 실패 중단 규칙을 실행한다.
- 완료 조건: 전체 454명 감사 범위와 출처별 성공·미확인·차단 수가 재현 가능한 보고서로 남고, 안전하게 확정된 누락만 보강되며, 데이터·검색·커버리지·빌드 및 바탕화면 `--check`가 통과한다.
