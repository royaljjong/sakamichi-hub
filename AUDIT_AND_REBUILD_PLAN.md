# Sakamichi Box 감사 및 재설계 계획

> **역사 기준선 + 후속 결정 기록**: §3~§5의 299명 수치와 실행 순서는 2026-08-20 감사 당시 상태입니다. 현재 제품 줄기는 `PRODUCT_RMVP_PLAN.md`, 실행·검증은 `WORK_ORDER_RMVP_2026-08-25.md`, 운영 상태는 `CODEX_HANDOVER.md`를 우선합니다. §6 이후는 후속 결정 이력으로 보존합니다.

기준일: 2026-08-20

## 0. 확정된 제품 결정

- 공식 로고는 일본어·영문 표기를 모두 허용한다.
- 로고는 4:3 공통 캔버스, `contain`, 동일 여백으로 정규화한다.
- Google 이미지 검색은 발견 용도이며 등록에는 공식 원본 URL·확인일·권리 상태가 필요하다.
- 순위는 X 팔로워, Instagram 팔로워, Google Trends 검색 관심도를 별도 TOP 10으로 공개한다. 플랫폼을 합산한 임의 인기 점수는 만들지 않는다.
- Google AdSense는 2026-08-25 기준 승인되어 실제 광고가 게재 중이다. 개인정보 처리방침과 운영 문서는 광고를 미래 상태로 표현하지 않으며, 광고 수익보다 사용자 신뢰·콘텐츠 가독성·정책 준수를 우선한다.

## 1. 큰그림

제품 목적은 사카미치·48그룹의 공식 멤버 정보, 공식 소식, 공연·공연장·티켓을 신뢰 가능한 출처와 함께 탐색하는 다국어 포털이다. 지하아이돌 지역 탐색 서비스와 데이터는 공유하지 않는다.

현재 가장 큰 충돌은 초기 작업지시서가 `사카미치 3그룹·사진/로고 금지`를 전제로 하지만, 현재 제품과 최신 사용자 요구는 `16개 그룹·공식 이미지·공연`을 포함한다는 점이다. 기존 작업지시서는 현행 계약으로 사용할 수 없다.

## 2. 중간그림

- 멤버: 사카미치 3그룹과 48그룹의 수집 완전성이 크게 다르다.
- 소식: 멤버 블로그와 운영 공지·공연·티켓 정보가 분리되지 않았다.
- 지역: 그룹의 본거지와 공연 개최지는 별도 축이어야 한다.
- 이미지: URL 존재 여부가 아니라 인물 일치·공식 출처·사용 조건을 검증해야 한다.
- 랭킹: 출처·플랫폼·수집일이 확인되기 전에는 공개하지 않는다.

## 3. 세부 감사 결과

- 16개 그룹, 299명, 업데이트 60건.
- 해외 48그룹은 그룹당 1~3명으로 전체 명단이 아니다.
- 멤버 이미지 URL 270건 중 27건은 로고 URL이다.
- 당시 `audit-report.json`은 2026-08-21 재실행분(299명 기준·미커밋)이었고 이미지 URL 상태 오판이 포함됐다. 2026-08-25에 454명 기준으로 갱신했지만 실행 환경의 URL status 0 경고는 여전히 인물·이미지 오염 판정에 사용하지 않는다.
- `link-report.json`은 303개 링크만 검사하며 사망 링크 6건이 남아 있다.
- 업데이트 9건은 `memberId`가 없어 작성자 연결을 보장하지 못한다.
- 공식 로고·단체 사진 데이터 계약이 없다.
- 검증된 랭킹 데이터가 없다.
- 미커밋 공연 데이터는 2개 장소·5개 공연에 한정된 수동 시드다.

## 4. 실행 순서

> 아래 순서는 당시 감사 계획이며 현재 작업 큐가 아닙니다. 완료·대체 여부는 현행 작업지시서와 인수인계를 확인합니다.

1. 잘못된 로고 이미지를 멤버 사진으로 렌더링하지 않는다.
2. 감사 보고서를 전체 299명·전체 링크 기준으로 재생성한다.
3. `member_blog`, `notice`, `concert`, `theater`, `ticket`을 분리한다.
4. 그룹 본거지와 공연장 위치를 분리한다.
5. 16개 그룹별 수집 완전성 상태와 마지막 확인일을 공개한다.
6. 공식 로고·사진은 출처와 사용 조건이 확인된 항목만 렌더링한다.
7. 공연 수집기를 그룹별 어댑터로 구축한다.
8. 랭킹은 검증된 스냅숏이 생긴 뒤 공개한다.
9. 마지막에 홈과 그룹 화면을 재설계한다.

## 5. 완료 기준

- 문서의 그룹·멤버 수와 실제 데이터가 일치한다.
- 전체 링크와 이미지에 검사 결과·확인일이 있다.
- UI에 추정 데이터가 사실처럼 노출되지 않는다.
- 공연에는 공식 URL, 일시, 장소, 티켓 상태가 있다.
- 그룹별 `complete / partial / collecting` 상태가 표시된다.

## 6. 2026-08-25 운영 정합성 보완

- 사용자 의도: 승인된 광고를 안정적으로 운영하면서 공개 문서와 실제 서비스 상태를 일치시킨다.
- 현재 문제: 개인정보 처리방침과 인수인계 일부가 AdSense를 `향후`, `활성화 후`, `승인 대기`로 표현한다. 반면 Publisher ID, `ads.txt`, 로더 스크립트는 이미 운영 값이다.
- 기대 흐름: 방문자는 현재 광고·쿠키 사용 가능성을 정확히 확인하고, 광고 설정에서 개인 맞춤 광고를 관리할 수 있다. 운영자는 승인 신청 절차 대신 승인 후 점검 항목을 따른다.
- 정책·안전 경계: 광고 클릭 유도, 콘텐츠를 가리는 배치, 출처가 불명확한 이미지 추가, 기존 자동 수집 데이터 덮어쓰기는 하지 않는다. 지역별 동의 메시지/CMP 설정은 AdSense 관리 화면의 실제 설정 확인 없이는 코드에서 완료로 단정하지 않는다.
- 이번 작업에서 바꾸지 않는 범위: 광고 슬롯 재배치, 신규 추적 도구, 멤버·이벤트·영상 데이터, 유료 API 도입.

### 후속 실행 결정

- 운영자가 직접 실행하던 갱신 명령 파일은 OneDrive로 이동된 실제 바탕화면에 복구하고, 저장소의 원본 배치 파일을 단일 원천으로 유지한다.
- 수집 단계가 실패했는데 오래된 데이터로 검증·커밋하는 흐름을 막기 위해 각 수집 실패 시 즉시 중단한다. 성공한 경우에도 지정된 자동 생성 JSON만 커밋한다.
- 현재 454명과 감사 보고서의 453명 불일치를 해소하되, URL 응답 실패는 인물 불일치 판정으로 사용하지 않고 감사 경고로만 남긴다.
- OG/Twitter URL은 페이지 메타데이터 자체에 프로덕션 기준 URL을 명시해 빌드 환경의 `localhost` 폴백을 제거한다.
