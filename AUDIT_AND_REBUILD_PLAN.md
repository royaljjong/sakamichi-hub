# Sakamichi Box 감사 및 재설계 계획

기준일: 2026-08-20

## 0. 확정된 제품 결정

- 공식 로고는 일본어·영문 표기를 모두 허용한다.
- 로고는 4:3 공통 캔버스, `contain`, 동일 여백으로 정규화한다.
- Google 이미지 검색은 발견 용도이며 등록에는 공식 원본 URL·확인일·권리 상태가 필요하다.
- 순위는 X 팔로워, Instagram 팔로워, Google Trends 검색 관심도를 별도 TOP 10으로 공개한다. 플랫폼을 합산한 임의 인기 점수는 만들지 않는다.

## 1. 큰그림

제품 목적은 사카미치·48그룹의 공식 멤버 정보, 공식 소식, 공연·공연장·티켓을 신뢰 가능한 출처와 함께 탐색하는 다국어 포털이다. 지하아이돌 지역 탐색 서비스와 데이터는 공유하지 않는다.

현재 가장 큰 충돌은 초기 작업지시서가 `사카미치 3그룹·사진/로고 금지`를 전제로 하지만, 현재 제품과 최신 사용자 요구는 `16개 그룹·공식 이미지·공연`을 포함한다는 점이다. 기존 작업지시서는 현행 계약으로 사용할 수 없다.

## 2. 중간그림

- 멤버: 사카미치 3그룹과 48그룹의 수집 완전성이 크게 다르다.
- 소식: 멤버 블로그와 운영 공지·공연·티켓 정보가 분리되지 않았다.
- 지역: 그룹의 본거지와 공연 개최지는 별도 축이어야 한다.
- 이미지: URL 존재 여부가 아니라 인물 일치·공식 출처·사용 조건을 검증해야 한다.
- 랭킹: 출처·플랫폼·수집일이 확인되기 전에는 공개하지 않는다.

## 3. 세부 감사 결과

- 16개 그룹, 299명, 업데이트 60건.
- 해외 48그룹은 그룹당 1~3명으로 전체 명단이 아니다.
- 멤버 이미지 URL 270건 중 27건은 로고 URL이다.
- 기존 `audit-report.json`은 176명 기준으로 현재 데이터와 불일치한다.
- `link-report.json`은 303개 링크만 검사하며 사망 링크 6건이 남아 있다.
- 업데이트 9건은 `memberId`가 없어 작성자 연결을 보장하지 못한다.
- 공식 로고·단체 사진 데이터 계약이 없다.
- 검증된 랭킹 데이터가 없다.
- 미커밋 공연 데이터는 2개 장소·5개 공연에 한정된 수동 시드다.

## 4. 실행 순서

1. 잘못된 로고 이미지를 멤버 사진으로 렌더링하지 않는다.
2. 감사 보고서를 전체 299명·전체 링크 기준으로 재생성한다.
3. `member_blog`, `notice`, `concert`, `theater`, `ticket`을 분리한다.
4. 그룹 본거지와 공연장 위치를 분리한다.
5. 16개 그룹별 수집 완전성 상태와 마지막 확인일을 공개한다.
6. 공식 로고·사진은 출처와 사용 조건이 확인된 항목만 렌더링한다.
7. 공연 수집기를 그룹별 어댑터로 구축한다.
8. 랭킹은 검증된 스냅숏이 생긴 뒤 공개한다.
9. 마지막에 홈과 그룹 화면을 재설계한다.

## 5. 완료 기준

- 문서의 그룹·멤버 수와 실제 데이터가 일치한다.
- 전체 링크와 이미지에 검사 결과·확인일이 있다.
- UI에 추정 데이터가 사실처럼 노출되지 않는다.
- 공연에는 공식 URL, 일시, 장소, 티켓 상태가 있다.
- 그룹별 `complete / partial / collecting` 상태가 표시된다.
