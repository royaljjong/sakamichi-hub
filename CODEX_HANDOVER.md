# Sakamichi Box — Codex 인수인계

기준일: 2026-08-21  
작업 브랜치: `codex/trust-data-phase2`  
프로덕션: https://sakamichi-hub.vercel.app

## 1. 제품 범위

Sakamichi Box는 지역 탐색 서비스가 아니다. 첫 화면에서 `사카미치 / AKB48 그룹`을 나누고, `계열 → 그룹 → 현재 멤버·연습생·졸업자 → 공식 SNS`로 탐색하는 다국어 비공식 디렉터리다.

홈 구성은 계열 선택, 그룹 카드, 사카미치 이벤트 슬라이더, AKB48 이벤트 슬라이더, 사카미치 생일 슬라이더, AKB48 생일 슬라이더 순서다. 그룹 선택 시 해당 그룹 색과 움직이는 배경을 사용한다.

## 2. 현재 데이터

- 그룹 16개
- 멤버 299명
- 현재/졸업/이탈 상태가 혼재
- 검증된 포털 이벤트 5건, 공연장 2건
- 검증된 랭킹 스냅숏 0건
- 공식 블로그 피드는 사카미치 실제 수집 항목만 노출
- AKB 고정 예시 게시물은 배포 피드에서 제외

## 3. 완료된 구현

- 지역·지도 UI 제거
- 사카미치/AKB48G 첫 단계 분리
- 그룹별 동적 색상·움직이는 배경
- 계열별 동일 비율 이벤트 레일
- 계열별 월간 생일 레일
- 현재/연습생/졸업자 탭 구조
- 공식 출처·수집일·권리 상태 데이터 계약
- 미검증 랭킹 및 잘못된 로고 이미지 노출 차단
- 타입 검사, 데이터 검사, 1,056페이지 프로덕션 빌드 통과

## 4. 자동 업데이트 매크로

예약 실행은 아직 `main`의 옛 커밋 `1ac841d`를 사용한다. 해당 워크플로의 `GITHUB_TOKEN`은 `Contents: read`라 수집과 커밋 이후 push 단계에서 403으로 실패한다.

수정된 작업 브랜치의 수동 실행 `32375223818`은 수집·커밋·push까지 성공했다. 완전 해결 조건은 작업 브랜치를 `main`에 병합한 후 `main`에서 수동 실행을 다시 성공시키는 것이다.

## 5. 확정된 사진 오염

- NMB48 11명과 STU48 16명이 동일한 STU48 로고 SVG를 인물 사진으로 사용
- AKB48의 向井地美音, 村山彩希, 岩立沙穂가 동일한 `83100622.jpg` 사용
- 총 30건을 UI에서 문자 아바타로 격리
- 원본 데이터 URL은 아직 남아 있으며 공식 프로필 대조 후 교체 또는 null 처리 필요

기존 `HEAD` 기반 감사는 공식 사이트의 봇 차단까지 장애로 판정하므로 `270/270 실패` 결과를 사진 삭제 근거로 사용하면 안 된다.

## 6. 다음 실행 순서

1. `codex/trust-data-phase2`를 `main`에 병합하고 예약 매크로 검증
2. 사카미치 3그룹 현역 사진·이름·상태·SNS 전수 대조
3. 사카미치 졸업자 전수 대조
4. AKB48 본점 현역·연구생·졸업자 대조
5. 일본 48 자매그룹
6. 해외 48 자매그룹
7. 공식 이벤트·포스터·티켓 수집 어댑터
8. X/Instagram/Google Trends 순위 스냅숏

각 멤버는 공식 프로필 URL, 표시 이름, 사진 URL, 상태, SNS, 확인일을 한 묶음으로 검증한다. 검색 결과 이미지나 이름 추정으로 교체하지 않는다.

## 7. 현재 미커밋 파일

- `data/audit-report.json`: 새 감사 실행 결과지만 URL 상태 오판이 포함돼 그대로 배포하면 안 됨
- `scripts/validate.ts`: 로고·중복 이미지 경고 추가
- `src/components/member/MemberAvatar.tsx`: 확정 오염 이미지 격리
- `PHASE2_HANDOVER.md`: 초기 Phase 2 메모
- `CODEX_HANDOVER.md`: 이 문서

