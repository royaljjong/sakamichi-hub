# Phase 2 정합성 재감사 인수인계

기준일: 2026-08-21

## 자동 업데이트 매크로

- 예약 실행은 `main`의 커밋 `1ac841d`를 사용하고 있어 `GITHUB_TOKEN Contents: read` 상태다.
- 따라서 데이터 수집과 자동 커밋까지 성공한 뒤 push에서 403으로 실패한다.
- 수정된 `codex/trust-data-phase2` 브랜치의 수동 실행 `32375223818`은 수집·커밋·push까지 성공했다.
- 해결 완료 조건은 수정 브랜치를 `main`에 병합한 뒤 `main`에서 수동 실행을 성공시키는 것이다.

## 멤버 사진 감사

- 데이터 299명, 이미지 URL 270건.
- 기존 `HEAD` 기반 URL 감사는 공식 사이트의 봇 차단을 장애로 오판하므로 사진 신원 검증에 사용할 수 없다.
- 확정 오염 1: NMB48 11명과 STU48 16명이 동일한 STU48 로고 SVG를 인물 사진으로 사용한다.
- 확정 오염 2: AKB48의 向井地美音, 村山彩希, 岩立沙穂가 동일한 `83100622.jpg`를 사용한다.
- 위 30건은 UI에서 즉시 문자 아바타로 격리했다. 데이터 URL 자체는 공식 프로필 대조 후 교체 또는 null 처리해야 한다.

## 재감사 순서

1. 사카미치 3그룹 현역
2. 사카미치 3그룹 졸업자
3. AKB48 현역·연구생
4. 일본 48 자매그룹
5. 해외 48 자매그룹

각 멤버는 공식 프로필 URL, 표시 이름, 사진 원본 URL, 수집일, 상태, SNS를 한 행으로 검증한다. 이름만 보고 이미지를 추정하거나 검색 결과 썸네일을 직접 등록하지 않는다.
